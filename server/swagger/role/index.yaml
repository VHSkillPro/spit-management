tags:
    - role
parameters:
    - $ref: '#/components/parameters/Authorization'
responses:
    200:
        description: Lấy danh sách chức vụ thành công
        content:
            application/json:
                schema:
                    type: object
                    properties:
                        data:
                            type: object
                            properties:
                                roles:
                                    $ref: '#/components/schemas/ListRole'
                        message:
                            type: string
                            example: Lấy danh sách chức vụ thành công
    401:
        $ref: '#/components/responses/Unauthorized'
    403:
        $ref: '#/components/responses/Forbidden'
    500:
        $ref: '#/components/responses/InternalServerError'